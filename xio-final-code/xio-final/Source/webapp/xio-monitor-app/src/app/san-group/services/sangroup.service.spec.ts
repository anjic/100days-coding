import { TestBed, inject } from '@angular/core/testing';

import { SangroupService } from './sangroup.service';

describe('SangroupService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SangroupService]
    });
  });

  // it('should ...', inject([SangroupService], (service: SangroupService) => {
  //   expect(service).toBeTruthy();
  // }));
});
