import { XioMonitorAppPage } from './app.po';

describe('xio-monitor-app App', () => {
  let page: XioMonitorAppPage;

  beforeEach(() => {
    page = new XioMonitorAppPage();
  });

  // it('should display message saying app works', () => {
  //   page.navigateTo();
  //   expect(page.getParagraphText()).toEqual('app works!');
  // });
});
