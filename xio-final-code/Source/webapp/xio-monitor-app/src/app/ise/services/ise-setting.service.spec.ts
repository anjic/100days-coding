import { TestBed, inject } from '@angular/core/testing';

import { IseSettingService } from './ise-setting.service';

describe('IseSettingService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IseSettingService]
    });
  });

  // it('should ...', inject([IseSettingService], (service: IseSettingService) => {
  //   expect(service).toBeTruthy();
  // }));
});
